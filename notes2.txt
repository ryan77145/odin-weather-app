location: 
response.address

date:
response.days.datetime

high: 
response.days.tempmax

low: 
response.days.tempmin

FeelsLike:
response.days.feelslike

conditions
response.days.conditions


generate.addEventListener("click", () => {
  async function fetchData() {
    const param = search.value;
    const encodedParam = encodeURIComponent(param);
    const response = await fetch(
      "https://api.giphy.com/v1/gifs/search?api_key=FlhBMiZEHs2UdmfUzRfcBdIXds7ZJpWX&q=" +
        encodedParam,
    );
    response
      .json()
      .then(function (response) {
        img.src =
          response.data[Math.floor(Math.random() * 50)].images.original.url;
      })
      .catch(function (error) {
        console.error(error);
      });
  }
  fetchData();
});